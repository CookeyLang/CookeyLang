class Country {
  construct(name, population) {
    this.name = name;
    this.population = population;
  }
}

class City extends Country {
  construct(name, population) {
    superClass.construct(name, population);
  }
}

var US = Country("United States", 500);
printLine(US.name);