include("math");

var n = 10;
var k = 5;
var t = 2;

printLine(Math.sin(Math.PI));
printLine(Math.cos(Math.PI));
printLine(Math.tan(Math.PI));
printLine(Math.E * n ^ (k * t));
printLine(Math.infinity);
printLine(Math.round(Math.sqrt(42)));
printLine(Math.round(Math.sqrtn(2, 42)));

%*
include("file");
%%File.createFolder("myfolder"); %% yup it works
%% then i delete the folder
var outcome = File.deleteFolder("myfolder");
printLine(outcome); %% works :D
*%